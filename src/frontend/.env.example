# Base URL del backend Spring Boot in dev locale (CORS abilitato lato BE — TSK-031).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Versione build esposta in UI (opzionale).
NEXT_PUBLIC_BUILD_VERSION=0.1.0-dev
