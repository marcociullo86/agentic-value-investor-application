---
id: TSK-095
title: Migration V013__filing_blob — tabella cache HTML filing 10-K/10-Q (renumbered from V011)
layer: db
consumer: agent
sprint: 7
priority: P1
estimate: XS
status: done
updated: 2026-05-25
epic: EP-011
story: US-039
wiki_pages:
  - wiki/runbooks/sec-10k-10q-analysis-playbook.md
  - wiki/concepts/webapp-architecture-vi.md
code_path: ./src/backend/src/main/resources/db/migration/
created: 2026-05-23
---
# TSK-095 — Migration V011__filing_blob — tabella cache HTML filing 10-K/10-Q

## Cosa fare

1. Creare `V011__filing_blob.sql` in `./src/backend/src/main/resources/db/migration/`.
2. La migrazione crea:
   ```sql
   CREATE TABLE filing_blob (
     id               BIGSERIAL PRIMARY KEY,
     ticker           VARCHAR(20)  NOT NULL,
     cik              VARCHAR(10),
     form_type        VARCHAR(10)  NOT NULL,
     accession_number VARCHAR(50),
     filed_at         DATE,
     html_blob        TEXT,
     chunkable_text   TEXT,
     fetched_at       TIMESTAMPTZ  NOT NULL DEFAULT now(),
     expires_at       TIMESTAMPTZ  NOT NULL,
     blob_size_bytes  INT,
     CONSTRAINT uq_filing_blob_accession UNIQUE (accession_number)
   );
   CREATE INDEX idx_filing_blob_ticker_form ON filing_blob (ticker, form_type, filed_at DESC);
   CREATE INDEX idx_filing_blob_expires ON filing_blob (expires_at);
   ```
3. Verificare applicazione senza errori su DB PostgreSQL 17 fresh + insieme alle migration precedenti.

## Acceptance Criteria

- [ ] `V011__filing_blob.sql` applicata da Flyway senza errori.
- [ ] La tabella `filing_blob` esiste con colonne e indici attesi.
- [ ] Il vincolo `UNIQUE (accession_number)` è applicato (test: insert duplicato → errore PK).
- [ ] `flyway validate` passa.

## Wiki page riferimento

[^src: wiki/runbooks/sec-10k-10q-analysis-playbook.md §Step 1 — Recupero filing]

## Note implementative

- `chunkable_text`: campo TEXT che memorizza l'HTML stripped (plain text) per evitare il parsing ad ogni accesso in US-040.
- `blob_size_bytes`: dimensione del `html_blob` in byte, usata per il limit 50MB di US-039.
- Stack: PostgreSQL 17 + Flyway 10.x. [^src: raw/tech_stack.md]
