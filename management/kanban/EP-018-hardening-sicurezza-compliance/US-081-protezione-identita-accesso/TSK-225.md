---
id: TSK-225
sprint: 15
layer: db
consumer: agent
priority: P0
estimate: XS
status: done
updated: 2026-05-27
us: US-081
depends_on: []
---
# TSK-225 — DB Migration V026__create_mfa_secrets

## Context
US-081 richiede tabella per memorizzare secret TOTP e recovery codes per MFA.
[^src: management/kanban/EP-018-hardening-sicurezza-compliance/US-081-protezione-identita-accesso/US-081.md §AC]
[^src: design_&_architecture/decisions/ADR-025-security-hardening-pci-dss.md §Schema DB]

## Technical Specs
- **DB:** Flyway migration `V026__create_mfa_secrets.sql` (V018 occupato da `V018__filing_analysis.sql`)
- **Schema:**
  ```sql
  CREATE TABLE mfa_secrets (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL UNIQUE REFERENCES users(id) ON DELETE CASCADE,
    totp_secret_encrypted VARCHAR(255) NOT NULL,
    enabled BOOLEAN NOT NULL DEFAULT false,
    enabled_at TIMESTAMPTZ,
    recovery_codes_hash TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now()
  );
  ```

## Implementation Steps
1. Creare file migration `V026__create_mfa_secrets.sql`
2. Verificare migration eseguita senza errori su Testcontainers

## Definition of Done
- [ ] Migration eseguita senza errori
- [ ] Tabella mfa_secrets creata con vincoli
- [ ] FK verso users con ON DELETE CASCADE
- [ ] UNIQUE su user_id
- [ ] Code review approvata

## Dependencies
Nessuna.
