---
id: TSK-226
sprint: 15
layer: db
consumer: agent
priority: P0
estimate: XS
status: done
updated: 2026-05-27
us: US-081
depends_on: []
---
# TSK-226 — DB Migration V025__create_login_attempts + indici

## Context
US-081 richiede tabella login_attempts per rate limiting e brute force protection.
[^src: management/kanban/EP-018-hardening-sicurezza-compliance/US-081-protezione-identita-accesso/US-081.md §AC]
[^src: design_&_architecture/decisions/ADR-025-security-hardening-pci-dss.md §Schema DB]

## Technical Specs
- **DB:** Flyway migration `V025__create_login_attempts.sql` (V019 occupato da `V019__deep_analysis_event_log.sql`)
- **Schema:**
  ```sql
  CREATE TABLE login_attempts (
    id BIGSERIAL PRIMARY KEY,
    ip_address VARCHAR(45) NOT NULL,
    account_email VARCHAR(255),
    attempted_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    success BOOLEAN NOT NULL,
    failure_reason VARCHAR(100),
    user_agent VARCHAR(500)
  );
  CREATE INDEX idx_login_attempts_ip_recent ON login_attempts (ip_address, attempted_at DESC);
  CREATE INDEX idx_login_attempts_email_recent ON login_attempts (account_email, attempted_at DESC) WHERE account_email IS NOT NULL;
  ```
- **Retention:** pulizia automatica > 90 giorni (scheduled task, in TSK-230)

## Implementation Steps
1. Creare file migration `V025__create_login_attempts.sql`
2. Creare tabella con indici
3. Verificare migration su Testcontainers

## Definition of Done
- [ ] Migration eseguita senza errori
- [ ] Tabella con indici creata
- [ ] Indice parziale su email funzionante
- [ ] Code review approvata

## Dependencies
Nessuna.
