---
id: TSK-025
title: "DB — Migration V006 (moat_checklist_entry)"
sprint: 3
layer: db
consumer: agent
priority: medium
estimate: 1h
status: done
depends_on: [TSK-001]
review_status: passed
review_iter: 1
review_report: code_quality/reports/TSK-025-iter-1.md
updated: 2026-05-27
---
# TSK-025 — Flyway migration V006: moat_checklist_entry

## Obiettivo

Creare la migration per la tabella che persiste le annotazioni Moat utente.

## Scope tecnico

- `V006__create_moat_checklist.sql`:
  - Tabella `moat_checklist_entry`: `id UUID PK`, `user_id UUID NOT NULL FK → users(id)
    ON DELETE CASCADE`, `ticker VARCHAR(10) NOT NULL FK → stocks(ticker)`,
    `moat_type VARCHAR(40) NOT NULL CHECK (moat_type IN ('INTANGIBLE_ASSETS',
    'SWITCHING_COSTS', 'NETWORK_EFFECT', 'COST_ADVANTAGE'))`,
    `status VARCHAR(20) NOT NULL CHECK (status IN ('PRESENT', 'PARTIAL', 'ABSENT'))`,
    `note TEXT NULL`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`.
    [^src: design_&_architecture/data/er-diagram.md §moat_checklist_entry]
  - Vincolo unique: `(user_id, ticker, moat_type)`.

## Criteri di completamento (DoD)

- [ ] V006 applica su schema con V001-V005 senza errori.
- [ ] Inserimento duplicato `(user_id, ticker, moat_type)` → unique constraint violation.
- [ ] `flyway validate` pulito.
